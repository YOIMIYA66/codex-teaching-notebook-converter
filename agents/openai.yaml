interface:
  display_name: "Teaching Notebook Converter"
  short_description: "Turn existing notebooks into validated visual lessons."
  default_prompt: "Convert this existing engineering notebook into a teaching copy. Preserve its executable workflow, select an appropriate quick, standard, or full mode, use imagegen for final direct-use text-bearing teaching visuals, add narrative guidance and inline HTML cards, and produce inspection, manifest, prompt-pack, and validation artifacts."
